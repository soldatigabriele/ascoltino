BOT_TOKEN=1234567890:AAAAA0000
# Model name(s) - comma-separated for multi-config debug mode (e.g., base,small,medium)
BOT_MODEL=base
LANGUAGE=it
# Beam size(s) - comma-separated for multi-config debug mode (e.g., 1,3,5)
BEAM_SIZE=1
# VAD filter(s) - comma-separated for multi-config debug mode (e.g., true,false)
VAD_FILTER=false
# Thread count(s) - comma-separated for multi-config debug mode (e.g., 2,4,8)
THREADS=0
BOT_NAME=
# Optional: receive startup notification in this chat
ADMIN_CHAT_ID=

# Multi-config mode example:
# BOT_MODEL=base,small
# BEAM_SIZE=1,5
# VAD_FILTER=true,false
# This would test 8 combinations (2 models × 2 beams × 2 vads)