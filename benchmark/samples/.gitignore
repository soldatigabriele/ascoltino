# Add your Italian audio samples here
#
# Supported formats: .wav, .mp3, .ogg, .oga, .m4a, .flac, .opus
#
# For WER (Word Error Rate) calculation, create a .txt file
# with the same name as the audio file containing the ground truth transcript.
#
# Example:
#   sample1.wav      - Audio file
#   sample1.txt      - Ground truth: "ciao come stai oggi"
#
# Tips:
#   - Use lowercase, no punctuation in ground truth for accurate WER
#   - Include various audio lengths (5s, 30s, 60s)
#   - Include clean and noisy samples for comprehensive testing

*
!.gitignore