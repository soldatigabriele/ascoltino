services:
  telegram-bot:
    build: .
    env_file:
      - .env
    volumes:
      - .:/app  # Mount code for live changes
      - ./logs:/app/logs
      - whisper-cache:/root/.cache

volumes:
  whisper-cache:
